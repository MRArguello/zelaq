import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as ButtonStories from './Button.stories';

<Meta of={ButtonStories} />

# Button

A pressable button. Use `primary` for the main action in a given context, `secondary` for
everything else, `link` when it should read as an inline link but still behave like a button
(same tap target, no navigation) — for actual navigation, use `Link` instead.

<Canvas of={ButtonStories.Primary} />

<Controls of={ButtonStories.Primary} />

## Tokens

- `colors.primary` — primary: background
- `colors.primaryDisabled` — primary: background (disabled)
- `colors.textOnPrimary` — primary: label
- `colors.secondaryBackground` — secondary: background
- `colors.secondaryBorder` — secondary: border
- `colors.secondaryText` — secondary: label
- `colors.primary` — link: label
- `opacity.disabled` — all: disabled dimming
- `radii.xs` — all: corner radius
- `space.base` — all: horizontal padding
- `space.md` — all: vertical padding, icon gap
- `sizes.touchMin` — all: minimum tap target

## Variants

<Canvas of={ButtonStories.Secondary} />
<Canvas of={ButtonStories.Link} />

## Icons

`startIcon`/`endIcon` accept any rendered element — sized and colored by you, no bundled icon
library. They're automatically treated as decorative (hidden from assistive tech); `Button`
already has an accessible name from its text label.

<Canvas of={ButtonStories.WithStartIcon} />

```tsx
// web
import { Search } from 'lucide-react';
<Button startIcon={<Search size={16} />}>Search</Button>

// React Native
import { Search } from 'lucide-react-native';
<Button startIcon={<Search size={16} color="#ffffff" />}>Search</Button>
```

## accessibilityHint

Supplemental context for when the result of an action isn't obvious from the label alone — not
limited to icon-only controls. `accessibilityLabel="Delete Account"` is clear about *what*
happens but not how consequential it is, so a hint still adds real information. Don't restate the
label, and don't use it as a substitute for a real label.

On React Native it forwards straight to `Pressable`'s own `accessibilityHint`. On web there's no
native hint concept, so it's rendered as a visually-hidden element linked via `aria-describedby`
— the label and the hint stay two separate screen-reader announcements.

<Canvas of={ButtonStories.WithAccessibilityHint} />

## Extra props

Any other native `<button>` (web) / `Pressable` (native) prop is accepted and forwarded — `name`,
`form`, `onKeyDown`, `hitSlop`, etc. Reserved and not overridable this way: `style`, `disabled`,
`type` (fixed to `"button"`, so it never accidentally submits a form), and the press handlers
(`onMouseDown`/`onMouseUp`/`onMouseLeave` on web, `onPressIn`/`onPressOut` on native) that drive the
pressed-state animation — use `onPress` for interaction. Passed-in `aria-*`/`accessibility*` props
also can't override the ones `Button` computes from `accessibilityLabel`/`accessibilityHint`.
