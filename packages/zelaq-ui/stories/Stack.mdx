import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as StackStories from './Stack.stories';

<Meta of={StackStories} />

# Stack

A theme-aware vertical layout primitive — a flexbox column for consistent spacing between
children. Prefer it over ad-hoc margins whenever you're stacking elements.

<Canvas of={StackStories.Basic} />

<Controls of={StackStories.Basic} />

## Tokens

- `space.sm`/`md`/`2md`/`base`/`lg`/`xl`/`2xl`/`3xl`/`4xl` — `gap` prop, defaults to `base`

## Gap values

<Canvas of={StackStories.GapValues} />

## Alignment and justification

<Canvas of={StackStories.CenterAligned} />
<Canvas of={StackStories.SpaceBetween} />
